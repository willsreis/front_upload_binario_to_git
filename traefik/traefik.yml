entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false

certificatesResolvers:
  letsencrypt:
    acme:
      email: seu-email@empresa.com
      storage: acme.json
      httpChallenge:
        entryPoint: web
